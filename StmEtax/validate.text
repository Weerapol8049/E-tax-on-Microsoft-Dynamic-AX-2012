boolean validate()
{
    boolean     let = true;
    if (isSend)
    {
        setPrefix("E-tax message");

        //if (custInvoiceJour)
        //{
            //if (StmEtaxTable::findCustInvoiceJour(custInvoiceJour.RecId, documentType))
                //let = checkFailed(strFmt("Can't send E-tex invoice '%1'", custInvoiceJour.InvoiceId));
        //}
        //else
            //if (projInvoiceJour)
        //{
            //if (StmEtaxTable::findProjInvoiceJour(projInvoiceJour.RecId))
                //let = checkFailed(strFmt("Can't send E-tex invoice '%1'", projInvoiceJour.ProjInvoiceId));
        //}
        //else
            //if (custInvoiceTable)
        //{
            //if (StmEtaxTable::findCustInvoiceTable(custInvoiceTable.RecId))
                //let = checkFailed(strFmt("Can't send E-tex invoice '%1'", custInvoiceTable.InvoiceId));
        //}
    }


    return let;
}
